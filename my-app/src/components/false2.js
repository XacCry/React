import { Table, TableBody, TableContainer, TableHead } from "@mui/material";
import { e } from "mathjs";
import React from "react";


const xl , xr , xm ,error,i;
const parser ;
var arr[];
class False2 extends React.Component{
    constructure(props){
        this.state = {xl =" " , xr = " " , xm = " "};
        this.handleSubmit;
        this.handlechange;
    }
    false2calculationfuction(){
        if(){
            while{
                arr.push(XR : xr , XL : xl , Error : error , Iteration :i);
            }
        }
        render(
            <TableContainer>
                <TableHead>
                    <Tablecell>
                        
                    </Tablecell>
                </TableHead>
                <TableBody>
                    <TableCell>

                    </TableCell>
                </TableBody>
            </TableContainer>

            return(
                <Chart>
                    <Series >

                    </Series>
                </Chart>
            )
        )
    }
}
handlechange(event){
    this.setState(this.handleSubmit(e.target.value : this.value))
};
handleSubmit(event){
    const{xl,xr,error} = this.state;
};
render(
    <Form>
        <div>
            <input>XR</input>

            <input>XL</input>

            <input>ERROR</input>

        </div>
        <div>
            <intput>function</intput>
        </div>
        <div>
            <button>Submit</button>
        </div>
    </Form>
)

export default False2;